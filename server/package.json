{
  "name": "shoe-view",
  "version": "1.0.0",
  "description": "describe api",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "cd server && npm install && node server.js",
    "heroku-postbuild": "cd shoe-client && yarn install && yarn run build"
  },
  "author": "Anthony Tweed",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cloudinary": "^1.14.0",
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.16.4",
    "mongoose": "^5.4.4",
    "multer": "^1.4.1",
    "multer-storage-cloudinary": "^2.2.1"
  },
  "devDependencies": {
    "morgan": "^1.9.1",
    "nodemon": "^1.18.9"
  },
  "engines": {
    "node": "10.13.0"
  }
}
